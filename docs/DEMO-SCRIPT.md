# AccessCrash 2:40 Build Week Demo

This is the exact narrative contract used for the published competition video.
The video is [publicly addressable on YouTube](https://youtu.be/aNcP-um0aZk)
and demonstrates the signed-out build at
[accesscrash.e-vigelis.chatgpt.site](https://accesscrash.e-vigelis.chatgpt.site).

## Delivery lock

- Runtime: exactly 160 seconds, below the three-minute limit.
- Picture: 1920×1080, H.264, `yuv420p`, 30 fps.
- Audio: AAC, 48 kHz stereo, OpenAI voice Marin, −16.01 LUFS.
- Captions: 36 exact English cues.
- Safe area: at least 6% on the left and right; critical proof stays above the
  bottom caption exclusion area.
- Data: fictional Pineglass process and synthetic capability profiles only.
- Public runtime: transparent fallback; the controlled live GPT evidence is
  shown separately and is never presented as the public run.

## 0–13 seconds — The gap between eligibility and access

**Narration**

> A student can qualify for help and still have no workable path to apply. AccessCrash finds where an education process becomes unreachable before a real person pays the price.

**Screen**

- AccessCrash hero and the complete tagline **Eligibility is not access.**
- The first visible title character remains at least 120 pixels from the edge.

## 13–28 seconds — Synthetic scope

**Narration**

> This is the fictional Pineglass Institute Access Grant. It uses synthetic capability profiles, no real student data, and no application persistence, so every judge sees the same bounded case.

**Screen**

- The loaded Pineglass source.
- Fictional, synthetic, no-real-student-data, and no-persistence boundaries.

## 28–46 seconds — Public judge fallback is explicit

**Narration**

> The public judge build fails closed. With live model access disabled, it clearly loads the bundled synthetic fallback. It never claims that fallback came from pasted instructions, and it never exposes a paid model endpoint.

**Screen**

- The exact public fallback badge and unconfirmed draft state.
- A visible truth boundary separating fallback from the controlled evidence
  frame that follows.

## 46–66 seconds — Controlled GPT-5.6 evidence

**Narration**

> Separately, a controlled server-boundary test called GPT-5.6 Sol on the exact synthetic Pineglass source. It returned seven source-cited, unconfirmed steps, no verdict, and a visible warning that AccessCrash normalized this one demo case to its documented topology.

**Screen**

- Sanitized evidence only: GPT-5.6 Sol, 7 draft steps, 0/7 confirmations, 0
  model verdicts.
- Visible disclosure that only this exact demo case is normalized; general
  sources are not.

## 66–85 seconds — Human confirmation boundary

**Narration**

> A human then inspects citations, prerequisites, capability routes, duration, and availability. They can confirm the displayed graph or reject and recompile it. Confirmation does not certify the policy as fair, legal, or complete.

**Screen**

- Inspectable rule evidence.
- **Confirm as shown** and **Reject / recompile** controls without implying a
  graph editor.

## 85–108 seconds — Capability-twin crash test

**Narration**

> AccessCrash holds eligibility constant only as a synthetic assumption and changes functional capabilities. The standard profile reaches the outcome. The constrained twin does not. Deterministic graph code, not GPT, returns BLOCKED because no reachable route remains.

**Screen**

- Standard control versus capability-constrained twin.
- Deterministic `BLOCKED` for the constrained twin and no model verdict.

## 108–123 seconds — Exact blocker proof

**Narration**

> This is not a generic score. The result names the failed transition, its source rule, and the graph evidence. Incomplete or unprovable evidence returns UNKNOWN instead of a confident guess.

**Screen**

- Exact source-linked blocker and graph evidence.
- Explicit `UNKNOWN` boundary without pretending it occurred in the shown
  baseline result.

## 123–143 seconds — Bounded repair-set regression

**Narration**

> Now the demo tests three alternatives together in memory: email verification, mobile upload, and evening review. The same engine re-evaluates the constrained twin and returns REACHABLE. No real program, source, or student record is changed.

**Screen**

- All three alternatives applied together in memory.
- The exact `BLOCKED → REACHABLE` comparison for the same default twin.

## 143–153 seconds — Authority and build proof

**Narration**

> Codex built AccessCrash. GPT drafts. A human confirms. Deterministic code decides.

**Screen**

- Codex: builds and tests.
- GPT-5.6: drafts only.
- Human: confirms.
- Deterministic code: decides.
- Verified product commit `810a859`, 55 tests, 0 failures, production build
  passed, and 0 production audit vulnerabilities.

## 153–160 seconds — Close

**Narration**

> AccessCrash. Eligibility is not access. Test the path before people crash into it.

**Screen**

- Live app: <https://accesscrash.e-vigelis.chatgpt.site>
- Source: <https://github.com/pakales/accesscrash>
- AI voice disclosure and synthetic-data boundary.

## Final media evidence

- YouTube: <https://youtu.be/aNcP-um0aZk>
- Local candidate SHA-256:
  `0da9722f6ec0cbfef84a9a16b7e6211454ba32610a67de84b741aab91c946804`
- Automated media QA: 160.000 s, 1080p, 30 fps, −16.01 LUFS, no speech
  trimming, 0 decode errors.

Automated QA, transcript review, and visual frame inspection found no key,
organization ID, project ID, private account surface, real student data, or
unsupported claim of fairness, compliance, eligibility, or measured real-world
impact. A separate human headphone listen was not performed; loudness, silence,
speech-boundary, transcription, and decode checks passed.
